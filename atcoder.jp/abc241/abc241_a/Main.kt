fun main() {
	val count_push=3
	var now_number=0
	var test=0
	val a=readLine()!!.split(" ").map{it->it.toInt()}

	for(i in 1..count_push){
		now_number=a[now_number]
	}

	for(i in 1..10000){
		for(j in 1..10000){
			test=a[0]
		}
	}

	println(now_number)
    
}