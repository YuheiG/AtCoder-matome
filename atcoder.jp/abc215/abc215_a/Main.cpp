#include <bits/stdc++.h>
#define rep(i, n) for (int i = 0; i < (int)(n); i++)
#define ll long long
using namespace std;
using Graph = vector<vector<int>>;

int main() {
	string s;
	cin>>s;
	if(s=="Hello,World!"){
		cout<<"AC"<<endl;
	}else{
		cout<<"WA"<<endl;
	}
    return 0;
}